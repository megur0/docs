---
title: "Route53・SSL証明書・CloudFront - AWS"
updated: 2026-07-24
---

[TOP(About this memo))](../README.md) > [一覧(AWS)](./README.md) > Route53・SSL証明書・CloudFront


# Route53
* AWSのDNS管理機能。AWS上の各サービスを独自ドメインで利用する上で便利な機能が用意されている。
* (IMO) Route53で管理する最大のメリットは、ELBやSSL証明書などの他サービスと連携しやすくなる点だと思う。

## 料金
* Route53には無料枠が設定されていないため、若干の料金が発生する。
    * ホストゾーン1つにつき0.50USD/月
    * 100万件のクエリごとに0.400USD

## ホストゾーンとは
* ホストゾーン1つは、1個のドメイン(example.com)を管理するグループの単位。

## ドメインの取得
* Route53でもドメイン自体を取得できるが、維持コストや取得コストを比較すると他のドメイン登録サービスのほうが安く済むことが多い(?)。

## 外部で取得したドメインをRoute53で使う
* ドメイン登録サービス側でネームサーバーを、Route53のネームサーバーに設定する。先にAレコードを登録しないとエラーになることがある(?)。
* (参考) https://mel.onl/onamae-domain-aws-route-53/

## ELBを使う場合
* EC2を直接SSL化する(証明書を自分で取得する)のはコストがかかるため現実的ではない。そのため、AWS上で無料発行できる証明書が利用可能なELBやCloudFrontをEC2の前に立てる構成にする。
* その場合、AレコードはエイリアスとしてELB(`dualstack.〜.ap-〜.elb.amazonaws.com.`のようなもの)を設定し、ELB側でドメインを登録する。
* EC2に直接アクセスする用の内部ドメインは、`hoge.ドメイン名`など適当なサブドメインにしておく。

## Aliasレコード
* ALIASレコードはRoute53の内部で扱う特殊なタイプで、外から見るとただのAレコードに見える。一言で言えば「各AWSプロダクトで利用するDNS名専用の、CNAMEレコード風の挙動を実現するレコード」。
* (参考) https://dev.classmethod.jp/cloud/aws/amazon-route-53-alias-records/

# AWSでのSSL証明書の取得方法
* 代理店などから購入する(有料)
* ACM(AWS Certificate Manager)を使う(無料、ただしEC2にはインストール不可。ELB、CloudFront、API Gateway、Elastic Beanstalkにはインストール可能)
* Let's Encryptを使う(無料、ただしAmazon Linuxは未対応(?))

# AWSの自社認証局への移行(2021年頃の話)
* AWSのサービスは徐々にAWSの自社認証局から証明書を発行する方向へ移行が進んでいた。自社認証局の証明書に対応していないと、2021年3月以降にS3やCloudFrontの利用に影響が出る可能性がある、という案内があった。
* (IME) ルート証明書の管理はアプリケーションによって異なるため対応方法は一概には言えないが、実際にはレアケースでほぼ影響がないことが多い(?)。
* (参考) https://qiita.com/fukasawah/items/13f1657e1a055bc7d2c7

# CloudFront
* Amazon CloudFrontは、AWSのCDN(コンテンツデリバリネットワーク)サービス。コンテンツファイルをサーバーから直接配信せず、CDNを介してユーザーに配信する。
* 静的ファイルおよび動的コンテンツを、ユーザーに最も近いサーバーから配信できる。配信サーバーは自動的に選択されるため、利用者側で特別な設定は不要。
* 動画や音声のメディアストリーミングデータの配信にも対応。
* コンテンツのキャッシュにも対応。
* 使い道: サーバーの負荷軽減、動的コンテンツのキャッシュなど。
